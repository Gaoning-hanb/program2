# coursebook-digest 配置 —— 把下面 KEY 行替换成你的真实 API Key
# 默认 BASE_URL 指向学校中转站（api.llm.ustc.edu.cn）：同校同学填自己的中转站 key 即可
# 校外使用可改为 DeepSeek 官方地址：https://api.deepseek.com/v1（key 在 DeepSeek 开放平台创建）
# 注意：等号后直接粘 key，不要加引号、不要有空格

DEEPSEEK_API_KEY=sk-替换成你的DeepSeek密钥

# 下面一般不用动
DEEPSEEK_BASE_URL=https://api.llm.ustc.edu.cn/v1
DEEPSEEK_MODEL=deepseek-v4-flash-ascend
TOKENIZERS_PARALLELISM=false

# 数据与存储
# DATA_DIR=./data
# STORAGE=both
# EMBEDDING_MODE=default

# 解析
# DEFAULT_PARSER=auto

# ---- B站视频（coursebook video）----
# B站登录 cookie（SESSDATA）：配置后可直接抓视频的 AI 字幕（秒级）。
# 获取：浏览器登录B站 → F12 → 应用/存储 → Cookie → 复制 SESSDATA 的值。
# 留空也能用：无字幕的视频会自动下载音频、用本地 whisper 转写（GPU 优先）。
# BILIBILI_SESSDATA=
# 无字幕视频的本地转写模型：tiny/base/small/medium（默认 small；GPU 自动优先）
# WHISPER_MODEL=small